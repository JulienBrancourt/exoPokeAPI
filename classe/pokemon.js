export class Pokemon {
    constructor(nom, poids, taille, type, capacite, id, image) {
        this.nom = nom
        this.poids = poids
        this.taille = taille
        this.type = type
        this.capacite = capacite
        this.id = id
        this.image = image
    }
}